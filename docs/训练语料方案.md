# 训练语料(数据集)方案

> 微调的方向由**语料**决定,不由 LoRA/QLoRA 决定。本文说明这周用什么、以后怎么准备。

---

## 一、本周(验证管线):直接用内置 identity

**为什么**:Day 5.1 的目标是"**训练管线跑通不报错**",不是真学到能力。所以用 LLaMA-Factory 自带的 `identity` 数据集(几十条,改模型"我是谁"的自我认知),几分钟能跑完、效果一眼可见。

想让它真的改名,把占位符替换掉即可:
```bash
cd ~/SenceTime/LLaMA-Factory
sed -i '' 's/{{name}}/小Q助手/g; s/{{author}}/张三/g' data/identity.json
```
(macOS 的 sed 要加 `''`;Linux 去掉)

---

## 二、数据格式(LLaMA-Factory 支持两种)

**Alpaca 格式**(单轮指令,最常用):
```json
[
  {"instruction": "把这句翻译成英文", "input": "今天天气很好", "output": "The weather is nice today."}
]
```

**ShareGPT 格式**(多轮对话):
```json
[
  {"conversations": [
    {"from": "human", "value": "你好"},
    {"from": "gpt",   "value": "你好!有什么可以帮你?"}
  ]}
]
```

用哪种都行,`instruction`(问)+ `output`(答)是核心;`input` 可留空。

---

## 三、语料从哪来(三条路)

| 来源 | 说明 | 适合 |
|---|---|---|
| **公开数据集** | HF/ModelScope 上现成的中文指令集:`alpaca_gpt4_zh`、`belle`、`firefly`、`COIG` 等。LLaMA-Factory 已预注册很多,直接 `dataset: alpaca_zh_demo` 就能用 | 快速起步、通用能力 |
| **自建** | 收集你的业务 Q&A(客服记录、文档问答等),整理成 Alpaca 格式 | 垂直领域、真实需求 |
| **合成(self-instruct)** | 用强模型(Qwen-Max / GPT)批量生成"指令-回答"对,再人工筛 | 缺数据时快速扩量 |

**实操建议**:先用公开数据集跑通效果,再逐步替换成自建/合成的垂直语料。

---

## 四、如何让 LLaMA-Factory 认识你的语料

1. 把你的 `my_data.json` 放到 `LLaMA-Factory/data/`。
2. 在 `data/dataset_info.json` 里注册一条:
```json
"my_data": {
  "file_name": "my_data.json",
  "columns": {"prompt": "instruction", "query": "input", "response": "output"}
}
```
3. 训练时 `bash code/train.sh` 里把 `DATASET="my_data"` 即可。

---

## 五、语料规模参考

| 目标 | 条数 | 说明 |
|---|---|---|
| 验证管线 | 几十条 | identity 即可 |
| 改风格/人设 | 几百~几千 | LoRA 足够 |
| 垂直领域能力 | 几千~几万 | 质量比数量重要 |
| 通用指令遵循 | 几万~几十万 | 需较强算力(4090+) |

> 记住:**质量 > 数量**。100 条高质量 > 1000 条噪声。
