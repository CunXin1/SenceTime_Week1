# 推理 4 步管线详解(配合 inference.py)

> 用一句真实输入 `"你好"` 走完全程,所有 token id 都是从 Qwen2.5-1.5B 分词器真跑出来的。

## 全景图

```
messages(你的对话)
   │  ② apply_chat_template     包装成 ChatML 文本
   ▼
"<|im_start|>system\n...<|im_end|>\n<|im_start|>user\n你好<|im_end|>\n<|im_start|>assistant\n"
   │  ① tokenize                文字 → token id
   ▼
[151644, 8948, 198, 2610, ... , 77091, 198]   (20 个 token)
   │  ③ generate                自回归逐 token 生成
   ▼
[151644,...,77091,198] + [104307, 109194, ...]   (输入 + 新生成)
   │  ④ decode                  新 token → 文字
   ▼
"你好！有什么我可以帮你的吗？"
```

> 注:代码里注释把 tokenize 标为 ①、template 标为 ②,但**执行顺序是先 ② 后 ①**(先拼文本,再把文本切成 token)。下面按执行顺序讲。

---

## 第 ② 步:apply_chat_template —— 把对话包装成 ChatML

**做什么**:你给的是结构化消息:
```python
[{"role": "system", "content": "You are a helpful assistant."},
 {"role": "user",   "content": "你好"}]
```
模型不认识这种字典。`apply_chat_template` 把它拼成一段**带特殊标记的纯文本**(Qwen 用的格式叫 **ChatML**):
```
<|im_start|>system
You are a helpful assistant.<|im_end|>
<|im_start|>user
你好<|im_end|>
<|im_start|>assistant
```

**关键点**:
- `<|im_start|>` / `<|im_end|>` 是**边界标记**,告诉模型"一段发言从哪开始、到哪结束、是谁说的"(im = *instant message*)。
- 末尾那行 `<|im_start|>assistant\n` 是 `add_generation_prompt=True` 补上的——它像**给模型递话筒**:"轮到你(assistant)说了"。少了这行,模型不知道该接话还是该继续复述用户。
- **为什么必须套**:模型训练时看到的就是这个格式。你直接喂 `"你好"` 裸文本,它可能把你的话当成要续写的文档,而不是要回答的问题。**格式=触发"对话模式"的开关**。

---

## 第 ① 步:tokenize —— 文字变数字

**做什么**:模型只会算数字,不认字。tokenize 把上面那段文本切成**子词(token)**,再映射成**整数 id**。上例整段 = **20 个 token**,前几个:

| token id | 子词 | 说明 |
|---|---|---|
| 151644 | `<|im_start|>` | 特殊 token,整体一个 id(不拆) |
| 8948 | `system` | 角色名 |
| 198 | `Ċ` | 换行符 `\n`(Ċ 是它的显示形式) |
| 2610 | `You` | |
| 525 | `Ġare` | `Ġ` 代表前面有个空格 |
| ... | ... | |
| 108386 | `你好` | **"你好"两个字合成 1 个 token** |
| 151645 | `<|im_end|>` | 结束标记 |
| 77091 | `assistant` | |
| 198 | `Ċ` | 最后的换行 |

**关键点(为 Day 4 埋线)**:
- **中文"你好"= 1 个 token**(id 108386),但显示成 `ä½łå¥½` 这种乱码——因为 Qwen 用 **byte-level BPE**,先把汉字按 UTF-8 拆成字节再合并,乱码只是"字节的可打印形式",不影响还原。
- **特殊 token 是不可分割的整体**:`<|im_start|>`=151644,不会被切成 `<`、`|`、`im`... 它们的 id 都很大(15 万多),排在词表最后。
- 英文里 `Ġare`、`Ġa` 的 `Ġ` 表示**词前空格**——BPE 把空格也编进 token,这样解码时能还原空格。

代码里还做了两件事:
```python
inputs = tokenizer([text], return_tensors="pt").to(DEVICE)
```
- `return_tensors="pt"`:输出 PyTorch 张量(不是普通 list),模型才能吃。
- `.to(DEVICE)`:把数据搬到 GPU(mps/cuda),否则和模型不在一个设备会报错。
- 返回的 `inputs` 里还有 `attention_mask`(标记哪些位置是真实 token、哪些是填充)。

---

## 第 ③ 步:generate —— 自回归逐 token 生成

**做什么**:这是模型"思考"的地方。它**不是一次吐出整句**,而是一个循环:

```
看当前所有 token → 预测"下一个 token 最可能是什么" → 选一个接上去
      ↑___________________________________________________|
                         重复,直到遇到 <|im_end|> 或达上限
```

每一步,模型对**全词表 15 万个候选** 打分(logits),然后按策略选一个:

| 参数 | 作用 |
|---|---|
| `max_new_tokens=512` | 最多生成 512 个新 token 就停 |
| `do_sample=True` | 按概率**随机采样**(而非每次都选最高分),回答更自然多样 |
| `temperature=0.7` | 温度:越高越随机发散,越低越保守。0 = 每次选最高分(贪心) |
| `top_p=0.8` | 核采样:只在累计概率前 80% 的候选里挑,掐掉长尾胡话 |

**关键点**:
- `do_sample=True` 意味着**同样的问题每次答案略有不同**(有随机性)。想复现固定答案就设 `do_sample=False`(贪心解码)。
- 内部有 **KV cache**:算过的 token 不重复计算,所以生成是逐步加速的。这也是 GQA(Day 3)要优化的对象——KV cache 越小越省显存。
- `with torch.no_grad()`:推理不需要反向传播,关掉梯度记录,省内存又加速。

输出 `output[0]` 是**输入 + 新生成**拼在一起的完整 token 序列。

---

## 第 ④ 步:decode —— 数字变回文字

**做什么**:把新生成的 token id 翻译回人话。

```python
new_tokens = output[0][inputs.input_ids.shape[1]:]   # 切掉输入那 20 个,只留新生成的
answer = tokenizer.decode(new_tokens, skip_special_tokens=True)
```

**关键点**:
- **为什么要切片 `[inputs.input_ids.shape[1]:]`**:`generate` 返回的是"输入+输出"全序列,前 20 个是你的 prompt,不切掉就会把自己的问题也打印出来。
- `skip_special_tokens=True`:把 `<|im_end|>`、`<|im_start|>` 这些标记从最终文本里去掉,只留干净的回答。
- decode 是 tokenize 的逆运算:id → 子词 → 拼接 → 还原空格和字节 → 人类文字。

---

## 一句话串起来

> 你说的话先被 **② 包装成 ChatML 对话格式**,再 **① 切成 token 数字**喂给模型;模型 **③ 一个接一个地预测下一个 token** 直到说完;最后把这些数字 **④ 翻译回文字** 给你看。

**最容易忽略的两个点**:
1. "能对话"不是模型天生的,是 **ChatML 模板 + 指令微调**共同"包"出来的效果。
2. 模型本质只干一件事——**预测下一个 token**;所谓推理、写代码、扮演角色,都是这个能力在不同 prompt 下的表现。
