# 第 1 周总结报告:环境与大模型导论

> 状态:Mac 部分已完成;含 CUDA(nvidia-smi / 7B QLoRA)的部分标注"待 4090"。

---

## 一、本周概述

本周目标是从零搭建大模型开发环境,并深入理解 Qwen2.5 的设计哲学。由于本机为 MacBook(M4 Pro / 16GB / Apple Silicon,只有 MPS 无 CUDA),采取"**Mac 先行 + 4090 补齐**"策略:在 Mac 上用 `Qwen2.5-1.5B-Instruct` 走通"下载 → 推理 → 架构分析 → 分词实验 → LoRA 微调"全流程,并额外横向对比了 4 个模型;需要 CUDA 的部分(7B QLoRA / 高性能推理)留待下周用 RTX 4090 完成。

---

## 二、环境搭建 (Day 1)

- 安装 Miniconda,创建隔离环境 `llm_exp`(Python 3.10)。
- 安装 **MPS 版 PyTorch 2.12**(Mac 无 CUDA,MPS 是 Apple 的 GPU 加速)+ transformers / accelerate / tokenizers / modelscope / jupyter / **LangChain**。
- LLaMA-Factory 最新版要求 Python≥3.11,另建 `llama_factory`(py3.11)环境安装。
- 验证:`torch.backends.mps.is_available() == True`(Mac 上等价于 CUDA 可用);`torch.cuda.is_available() == False`(符合预期,非错误)。
- **待 4090**:nvidia-smi、CUDA 12.1 版 PyTorch、`cuda.is_available()==True`、vLLM/OpenCompass 安装。

---

## 三、模型下载与推理 (Day 2)

- 从 HuggingFace 下载 4 个模型:`Qwen2.5-1.5B`、`Qwen3-4B`、`Llama-3.2-3B`、`Gemma-4-E2B`(共 ~27GB)。
- 编写 `inference.py`,显式实现推理 4 步管线:**① tokenize → ② chat_template → ③ generate → ④ decode**。
- 用 3 个领域 prompt(代码生成 / 逻辑推理 / 角色扮演)测试,记录对话日志。
- 测试 `apply_chat_template`,理解 Qwen 的 **ChatML 格式**(`<|im_start|>...<|im_end|>`)是"对话能力"的隐藏开关。

---

## 四、架构深度剖析 (Day 3)

逐字段分析 `config.json`,编写 `count_params.py` 统计参数,并横向对比 4 个模型。核心理解:

- **GQA(分组查询注意力)**:多个 Query 头共享少数 KV 头(Qwen2.5 是 12 Q 共享 2 KV),缩小推理 KV cache、省显存、加速长文本,质量几乎不降。
- **RoPE(旋转位置编码)**:按位置旋转 Q/K 向量,点积只依赖相对距离,编码相对位置;`rope_theta` 越大越能外推长文本(Qwen2.5=100 万)。无可训练参数。
- **参数分布**:Qwen/Llama 的 MLP 占 65–75%;Qwen 词表(15.2 万)占比偏高;**Gemma 因 Per-Layer Embeddings,embedding 竟占 54%**。
- **架构趋同**:4 个模型都用 GQA + RoPE + SwiGLU + RMSNorm + tie embedding —— 当代 LLM 的共识范式。

---

## 五、Tokenizer 分词实验 (Day 4)

用 Jupyter Notebook 设计 12 个极端用例(中英混合、emoji、数学公式、长文本截断、特殊 token 等),并横向对比 4 个模型的分词器。核心发现:

- **中文更费 token**:一个汉字常 1–2 token,英文一词常 1 token → 中文 API 更贵、上下文更快满。
- **BPE vs SentencePiece**:Qwen/Llama 用 byte-level BPE,中文子词显示成乱码;Gemma 用 SentencePiece,中文子词直接可读。
- **同句中文平均 token**:Qwen 4.2 < Gemma 4.4 < Llama 5.6 —— Qwen/Gemma 对中文更友好。
- **Qwen2.5 与 Qwen3 共用同一分词器**(词表都是 151936)。

---

## 六、LLaMA-Factory 微调 (Day 5)

- 调研确认:LLaMA-Factory 官方只支持 CUDA/Ascend/ROCm,QLoRA 依赖 bitsandbytes(CUDA 专属)Mac 无法运行;最新版要求 Python≥3.11,故新建 `llama_factory`(py3.11)环境。
- ✅ **在 Mac MPS 上成功跑通普通 LoRA 训练**(非 QLoRA),无报错 —— 验收❹ 达成。
- ✅ **两次微调**:
  - `alpaca_zh_demo`(中文通用指令,100 条,**3 分 48 秒**)
  - `identity_penguin`(自我认知,91 条,**1 分 39 秒**)
- ✅ **微调效果验证**:身份从"Qwen / 阿里云"成功改为 **"Penguin AI / Ruibo Sun"**(中英文都生效,见 `deliverables/微调/微调前后对比.md`)。
- ✅ **LoRA 只存 35MB adapter**(基座 2.9GB 不变),实证增量微调本质。
- TensorBoard 日志已产出(`saves/*/runs/`),loss 曲线见 `deliverables/微调/*.png`。
- **待 4090**:按任务原意的 **7B QLoRA** 训练(`finetune.py` 改 MODEL/QLORA 即可)+ AWQ 压缩(`export_awq.py`)。

---

## 七、遇到的问题与解决

| 问题 | 解决 |
|---|---|
| Mac 无 CUDA,`cuda.is_available()==False` | 改用 MPS;CUDA 部分留 4090 |
| Gemma-4 推理输出退化、报"无 chat template" | Gemma 是多模态,需 `AutoProcessor` + 补下漏掉的 `chat_template.jinja` |
| Qwen3 思考模式输出冗长 | `enable_thinking=False` 关闭 |
| LLaMA-Factory 要 Python 3.11 | 新建 py3.11 专用环境 |
| Mac 无法 QLoRA(bitsandbytes 依赖 CUDA) | 改用普通 LoRA;7B QLoRA 留 4090 |

---

## 八、验收标准对照

| # | 标准 | 状态 |
|---|---|---|
| ❶ | CUDA 可用 | 🟡 待 4090(Mac 用 MPS) |
| ❷ | 基座模型能对话 | ✅ 完成 |
| ❸ | 口头解释 RoPE / GQA | ✅ 完成 |
| ❹ | LLaMA-Factory demo 无报错 | ✅ **完成(Mac MPS 上 LoRA 训练成功)** |
| ❺ | 周报提交 | ✅ 本报告 |

---

## 九、下周计划 (4090)

1. 装 CUDA 版 PyTorch,`nvidia-smi` 查规格,确认 `cuda.is_available()==True`。
2. 用 7B QLoRA 复跑训练(`finetune.py` 改 MODEL=7B、QLORA=4)。
3. AWQ 4-bit 压缩部署(`export_awq.py`)。
4. 补齐 nvidia-smi 截图,提交最终报告。

---

## 附:交付物清单

- `code/推理/`:inference.py, inference_gemma4.py, compare_inference.py
- `code/分析/`:count_params.py
- `code/微调/`:finetune.py, lf_lora.yaml, compare_finetune.py, export_awq.py
- `code/download_models.py`
- `deliverables/环境/`:conda_list.txt, env_report.md
- `deliverables/推理输出/`:4模型推理输出.md
- `deliverables/架构分析/`:Qwen2.5架构分析报告.md, 4模型参数统计.md, 4模型对比分析.md
- `deliverables/分词实验/`:tokenizer_experiments.ipynb
- `deliverables/微调/`:LoRA微调报告.md, 微调前后对比.md, loss 曲线图
- `docs/`:任务与约束、推理管线详解、现代LLM设计_中英、训练语料方案 等
