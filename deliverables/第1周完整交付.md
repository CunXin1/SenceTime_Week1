# 第 1 周完整交付文档

> 汇总本周所有交付内容。分词 notebook 见 `分词实验/tokenizer_experiments.ipynb`;4模型推理原文见 `推理输出/4模型推理输出.md`。

## 目录
1. 第1周总结报告(周报)
2. Qwen2.5 设计哲学
3. config.json 逐字段分析
4. Qwen2.5 架构分析报告(vs Llama3)
5. 4 模型对比分析
6. 4 模型参数统计
7. BPE vs SentencePiece 深度对比
8. LoRA 微调报告
9. 微调前后对比

# ═══ 1. 周报 ═══

# 第 1 周总结报告:环境与大模型导论

> 状态:Mac 部分已完成;含 CUDA(nvidia-smi / 7B QLoRA)的部分标注"待 4090"。

---

## 一、本周概述

本周目标是从零搭建大模型开发环境,并深入理解 Qwen2.5 的设计哲学。由于本机为 MacBook(M4 Pro / 16GB / Apple Silicon,只有 MPS 无 CUDA),采取"**Mac 先行 + 4090 补齐**"策略:在 Mac 上用 `Qwen2.5-1.5B-Instruct` 走通"下载 → 推理 → 架构分析 → 分词实验 → LoRA 微调"全流程,并额外横向对比了 4 个模型;需要 CUDA 的部分(7B QLoRA / 高性能推理)留待下周用 RTX 4090 完成。

---

## 二、环境搭建 (Day 1)

- 安装 Miniconda,创建隔离环境 `llm_exp`(Python 3.10)。
- 安装 **MPS 版 PyTorch 2.12**(Mac 无 CUDA,MPS 是 Apple 的 GPU 加速)+ transformers / accelerate / tokenizers / modelscope / jupyter / **LangChain**。
- LLaMA-Factory 最新版要求 Python≥3.11,另建 `llama_factory`(py3.11)环境安装;OpenCompass 另建 `opencompass` 环境安装(避免污染 llm_exp)。
- 验证:`torch.backends.mps.is_available() == True`(Mac 上等价于 CUDA 可用);`torch.cuda.is_available() == False`(符合预期,非错误)。
- **核心工具链**:LangChain ✅、LLaMA-Factory ✅、OpenCompass ✅(独立环境);vLLM ❌ 待 4090(CUDA 专属)。
- **待 4090**:nvidia-smi、CUDA 12.1 版 PyTorch、`cuda.is_available()==True`、vLLM 安装。

---

## 三、模型下载与推理 (Day 2)

- 从 HuggingFace 下载 4 个模型:`Qwen2.5-1.5B`、`Qwen3-4B`、`Llama-3.2-3B`、`Gemma-4-E2B`(共 ~27GB)。
- 编写 `inference.py`,显式实现推理 4 步管线:**① tokenize → ② chat_template → ③ generate → ④ decode**。
- 用 3 个领域 prompt(代码生成 / 逻辑推理 / 角色扮演)测试,记录对话日志。
- 测试 `apply_chat_template`,理解 Qwen 的 **ChatML 格式**(`<|im_start|>...<|im_end|>`)是"对话能力"的隐藏开关。

---

## 四、架构深度剖析 (Day 3)

逐字段分析 `config.json`(每个字段的完整解释见 `deliverables/架构分析/config逐字段分析.md`),编写 `count_params.py` 统计参数,并横向对比 4 个模型。核心理解:

- **GQA(分组查询注意力)**:多个 Query 头共享少数 KV 头(Qwen2.5 是 12 Q 共享 2 KV),缩小推理 KV cache、省显存、加速长文本,质量几乎不降。
- **RoPE(旋转位置编码)**:按位置旋转 Q/K 向量,点积只依赖相对距离,编码相对位置;`rope_theta` 越大越能外推长文本(Qwen2.5=100 万)。无可训练参数。
- **参数分布**:Qwen/Llama 的 MLP 占 65–75%;Qwen 词表(15.2 万)占比偏高;**Gemma 因 Per-Layer Embeddings,embedding 竟占 54%**。
- **架构趋同**:4 个模型都用 GQA + RoPE + SwiGLU + RMSNorm + tie embedding —— 当代 LLM 的共识范式。
- **Qwen2.5 设计哲学(任务核心)**:在成熟骨架之上,通过"**超大词表 + 激进 GQA(6:1)+ 大 rope_theta + 家族同构**",把重心压在"**中文友好 + 推理高效 + 可平滑缩放**"上——不追求架构创新,而追求把已知有效的设计组合成产品级、可低成本大规模部署的模型系列。详见 `deliverables/架构分析/Qwen2.5设计哲学.md`。

---

## 五、Tokenizer 分词实验 (Day 4)

用 Jupyter Notebook 设计 12 个极端用例(中英混合、emoji、数学公式、长文本截断、特殊 token 等),并横向对比 4 个模型的分词器。核心发现:

- **中文更费 token**:一个汉字常 1–2 token,英文一词常 1 token → 中文 API 更贵、上下文更快满。
- **BPE vs SentencePiece**:Qwen/Llama 用 byte-level BPE,中文子词显示成乱码;Gemma 用 SentencePiece,中文子词直接可读。
- **同句中文平均 token**:Qwen 4.2 < Gemma 4.4 < Llama 5.6 —— Qwen/Gemma 对中文更友好。
- **Qwen2.5 与 Qwen3 共用同一分词器**(词表都是 151936)。

---

## 六、LLaMA-Factory 微调 (Day 5)

- 调研确认:LLaMA-Factory 官方只支持 CUDA/Ascend/ROCm,QLoRA 依赖 bitsandbytes(CUDA 专属)Mac 无法运行;最新版要求 Python≥3.11,故新建 `llama_factory`(py3.11)环境。
- ✅ **在 Mac MPS 上成功跑通普通 LoRA 训练**(非 QLoRA),无报错 —— 验收❹ 达成。
- ✅ **两次微调**:
  - `alpaca_zh_demo`(中文通用指令,100 条,**3 分 48 秒**)
  - `identity_penguin`(自我认知,91 条,**1 分 39 秒**)
- ✅ **微调效果验证**:身份从"Qwen / 阿里云"成功改为 **"Penguin AI / Ruibo Sun"**(中英文都生效,见 `deliverables/微调/微调前后对比.md`)。
- ✅ **LoRA 只存 35MB adapter**(基座 2.9GB 不变),实证增量微调本质。
- ✅ **LoRA 合并**:用 `export_awq.py` 把 adapter 合并回基座 → 2.9GB 独立模型(可脱离基座部署,含 Ollama Modelfile)。AWQ 量化那步需 4090。
- ✅ **TensorBoard 可视化**:`tensorboard --logdir saves/` 启动,`localhost:6006` 查看两次训练 loss 曲线(Day 5.2)。
- **待 4090**:按任务原意的 **7B QLoRA** 训练(`finetune.py` 改 MODEL/QLORA 即可)+ AWQ 压缩(`export_awq.py`)。

---

## 七、遇到的问题与解决

| 问题 | 解决 |
|---|---|
| Mac 无 CUDA,`cuda.is_available()==False` | 改用 MPS;CUDA 部分留 4090 |
| Gemma-4 推理输出退化、报"无 chat template" | Gemma 是多模态,需 `AutoProcessor` + 补下漏掉的 `chat_template.jinja` |
| Qwen3 思考模式输出冗长 | `enable_thinking=False` 关闭 |
| LLaMA-Factory 要 Python 3.11 | 新建 py3.11 专用环境 |
| Mac 无法 QLoRA(bitsandbytes 依赖 CUDA) | 改用普通 LoRA;7B QLoRA 留 4090 |

---

## 八、验收标准对照

| # | 标准 | 状态 |
|---|---|---|
| ❶ | CUDA 可用 | 🟡 待 4090(Mac 用 MPS) |
| ❷ | 基座模型能对话 | ✅ 完成 |
| ❸ | 口头解释 RoPE / GQA | ✅ 完成 |
| ❹ | LLaMA-Factory demo 无报错 | ✅ **完成(Mac MPS 上 LoRA 训练成功)** |
| ❺ | 周报提交 | ✅ 本报告 |

---

## 九、下周计划 (4090)

1. 装 CUDA 版 PyTorch,`nvidia-smi` 查规格,确认 `cuda.is_available()==True`。
2. 用 7B QLoRA 复跑训练(`finetune.py` 改 MODEL=7B、QLORA=4)。
3. AWQ 4-bit 压缩部署(`export_awq.py`)。
4. 补齐 nvidia-smi 截图,提交最终报告。

---

## 附:交付物清单

- `code/inference/`:inference.py, inference_gemma4.py, compare_inference.py
- `code/analysis/`:count_params.py
- `code/finetune/`:finetune.py, lf_lora.yaml, compare_finetune.py, export_awq.py
- `code/download_models.py`
- `deliverables/环境/`:conda_list.txt, env_report.md
- `deliverables/推理输出/`:4模型推理输出.md
- `deliverables/架构分析/`:**Qwen2.5设计哲学.md**、**config逐字段分析.md**、Qwen2.5架构分析报告.md, 4模型参数统计.md, 4模型对比分析.md
- `deliverables/分词实验/`:tokenizer_experiments.ipynb、**BPE_vs_SentencePiece.md**
- `deliverables/微调/`:LoRA微调报告.md, 微调前后对比.md, loss 曲线图
- `docs/`:任务与约束、推理管线详解、现代LLM设计_中英、训练语料方案 等

# ═══ 2. 设计哲学 ═══

# Qwen2.5 设计哲学:为什么它这么设计

> 任务核心目标:不只是跑起来,而是**看懂 Qwen2.5 每一个设计决策背后的"为什么"**。
> 本文从"权衡(trade-off)"角度,逐个拆解它的设计意图,并用真实 config 数值佐证。

---

## 总纲:Qwen2.5 在优化什么?

一个模型的架构不是随便堆的,而是在**三个相互冲突的目标**之间做权衡:

```
        质量(能力强)
          /\
         /  \
        /    \
  推理成本 —— 训练/部署可行性
  (省显存/快)   (放得下/训得动)
```

**Qwen2.5 的取舍倾向非常明确:在"够强"的前提下,极力压低"推理成本",并特别照顾中文/多语言。** 它的每个设计决策几乎都能归到这条主线上。下面逐个印证。

---

## 决策 1:超大词表(151936)—— 为什么把词表做这么大?

**数值**:Qwen2.5 词表 151936,远大于 Llama3 的 128256。

**为什么**:
- **多语言/中文覆盖**:更大的词表能把常见中文词、日韩、代码符号直接收进单个 token,而不是拆成一堆字节。
- **省 token = 省钱 + 上下文更耐用**:我们分词实验实测——同一句中文,Qwen 平均 4.2 token,Llama 5.6 token。中文任务上 Qwen 每次调用更便宜、32K 上下文能装更多内容。

**代价(权衡)**:词表越大,embedding 矩阵越大。参数统计显示 **Qwen2.5-1.5B 的 embedding 占了 15.1%**(而更大的模型里这个比例会下降)。Qwen 认为"小模型多花一点在词表上、换来中文效率"这笔账划算。

**哲学**:**词表是留给中文/多语言用户的"效率红利",宁可多占一点参数。**

---

## 决策 2:激进的 GQA(6:1)—— 为什么比 Llama 更狠?

**数值**:Qwen2.5-1.5B = 12 个 Q 头共享 **2** 个 KV 头(6:1);Llama3.2 是 24Q/8KV(3:1)。Qwen 的分组比明显更激进。

**为什么**:
- 推理时 KV cache 是长文本的显存瓶颈。KV 头越少,cache 越小。Qwen 的 6:1 意味着 **KV cache 只有全 MHA 的 1/6**,比 Llama 的 1/3 更省。
- 这直接服务于**大规模、低成本部署**:同样显存能塞更长上下文、更多并发。

**代价**:KV 头太少理论上会略损质量(Q 头之间共享的 KV 信息更"粗")。Qwen 押注"6:1 的质量损失可忽略,省显存的收益远大"。

**印证**:参数统计里 **Qwen2.5 的 attention 只占 10%**(Llama 占 22%)——KV 投影矩阵被压得很小,正是激进 GQA 的物理体现。

**哲学**:**推理效率优先。宁可在注意力上更激进,换取部署时的显存和吞吐。**

---

## 决策 3:大 rope_theta(100万)—— 为什么把旋转基频拉这么高?

**数值**:Qwen2.5 rope_theta = 1,000,000;Llama3 = 500,000;初代 Llama = 10,000。

**为什么**:
- RoPE 的 theta 越大,位置向量旋转越慢,能"不绕圈"地编码越长的序列 → **长上下文外推更稳**。
- Qwen2.5 支持 32K(部分版本 128K)上下文,大 theta 是长文本能力的地基。

**哲学**:**为长文本场景(长文档、RAG、代码库)预留能力**,这也是现代 LLM 的军备竞赛方向。

---

## 决策 4:tie word embeddings —— 为什么小模型要共享词表权重?

**数值**:Qwen2.5-1.5B `tie_word_embeddings = True`(输入 embedding 与输出 lm_head 共享同一矩阵)。

**为什么**:
- 词表 151936 × hidden 1536 ≈ 2.33 亿参数。若输入/输出各存一份,光词表就占 4.6 亿,对 1.5B 模型是巨大浪费。
- 共享后只存一份,省下的参数可以分给真正学能力的层。

**注意**:大模型(如 72B)里词表占比小,tie 的收益下降,所以更大的 Qwen 版本不一定 tie。**这是"因模型大小而变"的务实选择。**

**哲学**:**在小模型上,每一份参数都要花在刀刃上。**

---

## 决策 5:SwiGLU + RMSNorm + Pre-Norm —— 为什么用这套"标准件"?

**数值**:`hidden_act = silu`(配门控即 SwiGLU),RMSNorm,前置归一化。

**为什么**:
- **SwiGLU**:比 ReLU/GELU 表达力强,同等参数下效果更好(代价是 MLP 有 3 个矩阵,所以 MLP 占了 ~75% 参数)。
- **RMSNorm**:比 LayerNorm 少算均值、无偏置,更快,效果相当。
- **Pre-Norm**:归一化放子层前,深层网络训练更稳。

**哲学**:**这些是经过验证的"最优标准件",不在这里冒险创新,把创新预算留给数据和规模。** 这也是为什么 Qwen/Llama/Gemma 骨架高度趋同。

---

## 决策 6:家族化 + 同构 —— 为什么 0.5B 到 72B 都长一个样?

**观察**:Qwen2.5 提供 0.5B/1.5B/3B/7B/14B/32B/72B 一整个家族,**架构同构、分词器相同**(我们实测 Qwen2.5 与 Qwen3 共用 tokenizer)。

**为什么**:
- **同构**:小模型和大模型架构一致,便于用小模型做实验、验证方案,再无缝放大到大模型(我们这周正是用 1.5B 验证,7B 只需改配置)。
- **同分词器**:上下游工具、数据处理、prompt 全家族通用,生态成本低。
- **全尺寸覆盖**:从端侧(0.5B)到云端(72B),让用户按"显存→模型"对号入座(正对应任务里 "8GB→1.5B,16GB→7B")。

**哲学**:**把模型做成一个可平滑缩放的"产品线",而非孤立的单点。**

---

## 决策 7:Instruct 版 + ChatML —— 对话能力从哪来?

**观察**:`-Instruct` 版经过指令微调;对话靠 **ChatML 模板**(`<|im_start|>role...<|im_end|>`)驱动。

**为什么**:
- 基座模型只会"续写下一个 token";对话能力是**指令微调 + 固定对话格式**共同"包"出来的。
- ChatML 用特殊 token 明确标注"谁在说、说到哪",让模型学会多轮对话的边界(我们在推理实验里亲眼看到了这套格式)。

**哲学**:**能力(基座)与对齐(Instruct/模板)解耦**——先训通用能力,再用轻量微调对齐到对话/指令。

---

## 一图总结:Qwen2.5 的设计主线

| 设计 | 表面是什么 | 背后为什么 | 服务的目标 |
|---|---|---|---|
| 超大词表 151936 | 词表大 | 中文/多语言省 token | 效率 + 多语言 |
| 激进 GQA 6:1 | KV 头少 | KV cache 小 6 倍 | 推理成本 |
| 大 rope_theta 1M | 旋转慢 | 长文本外推稳 | 长上下文能力 |
| tie embeddings | 词表共享 | 小模型省参数 | 参数效率 |
| SwiGLU/RMSNorm | 标准件 | 稳 + 强,不冒险 | 训练可行性 |
| 家族同构 | 全尺寸一个样 | 可平滑缩放 | 生态/产品化 |
| Instruct + ChatML | 会对话 | 能力与对齐解耦 | 可用性 |

**一句话概括 Qwen2.5 的设计哲学:**
> **在现代 LLM 的成熟骨架(RoPE+GQA+SwiGLU+RMSNorm)之上,通过"超大词表 + 激进 GQA + 大 rope_theta + 家族同构",把重心压在"中文友好 + 推理高效 + 可平滑缩放"上——它不追求架构创新,而追求把已知有效的设计,组合成一个能低成本、大规模、跨语言部署的产品级模型系列。**

# ═══ 3. config逐字段分析 ═══

# Qwen2.5-1.5B config.json 逐字段分析 (Day 3.1)

> 逐个解释 `models/Qwen2.5-1.5B-Instruct/config.json` 的**每一个字段**,重点标注任务要求的
> vocab_size / hidden_size / num_attention_heads / num_key_value_heads / rope_theta。

## 完整 config.json

```json
{
  "architectures": ["Qwen2ForCausalLM"],
  "attention_dropout": 0.0,
  "bos_token_id": 151643,
  "eos_token_id": 151645,
  "hidden_act": "silu",
  "hidden_size": 1536,
  "initializer_range": 0.02,
  "intermediate_size": 8960,
  "max_position_embeddings": 32768,
  "max_window_layers": 21,
  "model_type": "qwen2",
  "num_attention_heads": 12,
  "num_hidden_layers": 28,
  "num_key_value_heads": 2,
  "rms_norm_eps": 1e-06,
  "rope_theta": 1000000.0,
  "sliding_window": 32768,
  "tie_word_embeddings": true,
  "torch_dtype": "bfloat16",
  "transformers_version": "4.43.1",
  "use_cache": true,
  "use_sliding_window": false,
  "vocab_size": 151936
}
```

---

## 一、任务重点字段(5 个)

### ⭐ `vocab_size: 151936` —— 词表大小
- **含义**:模型认识 151936 个不同的 token。
- **推导**:embedding 矩阵 = vocab × hidden = 151936 × 1536 ≈ **2.33 亿参数**(占 1.5B 的 15.1%)。
- **意义**:词表很大(Llama3 才 128256),对中文/多语言友好,同样中文更省 token。

### ⭐ `hidden_size: 1536` —— 隐藏维度(模型"宽度")
- **含义**:每个 token 用 1536 维向量表示,是贯穿全模型的主干宽度。
- **推导**:`head_dim = hidden_size / num_attention_heads = 1536 / 12 = 128`(每个注意力头 128 维)。
- **意义**:宽度决定表达容量;1536 是 1.5B 这个体量的典型选择。

### ⭐ `num_attention_heads: 12` —— Query 头数
- **含义**:注意力有 12 个并行的 Query 头,每头 128 维,并行捕捉不同关系。

### ⭐ `num_key_value_heads: 2` —— KV 头数(GQA)
- **含义**:只有 2 个 Key/Value 头,被 12 个 Q 头共享 → **GQA,分组比 6:1**。
- **意义**:推理时 KV cache 只有全 MHA 的 **1/6**,大幅省显存、加速长文本。这是 Qwen2.5 attention 只占 10% 参数的直接原因。

### ⭐ `rope_theta: 1000000.0` —— RoPE 旋转基础频率
- **含义**:位置编码旋转的基频。越大 → 旋转越慢 → 能编码越长序列而不"绕圈"。
- **意义**:配合 `max_position_embeddings: 32768` 支撑长上下文;Llama3 才 500000,Qwen 更激进。

---

## 二、其余字段逐个解释

| 字段 | 值 | 含义 |
|---|---|---|
| `architectures` | `Qwen2ForCausalLM` | 用哪个模型类加载 —— **Qwen2.5 复用 Qwen2 架构类**(2.5 是 2 的数据/训练升级,架构同源) |
| `model_type` | `qwen2` | 模型类型标识,transformers 靠它找对应实现 |
| `num_hidden_layers` | 28 | 层数(模型"深度")。深度 × 宽度共同决定容量 |
| `intermediate_size` | 8960 | MLP 中间层维度,约 hidden 的 5.8 倍。SwiGLU 有 gate/up/down 三个矩阵,是参数大头(~75%) |
| `hidden_act` | `silu` | 激活函数;配门控即 **SwiGLU**,比 ReLU/GELU 表达力强 |
| `rms_norm_eps` | 1e-06 | **RMSNorm** 的极小值,防止除以零,保数值稳定 |
| `tie_word_embeddings` | true | 输入 embedding 与输出 lm_head **共享同一矩阵**,省下一份 2.33 亿参数(小模型关键优化) |
| `max_position_embeddings` | 32768 | 训练支持的最大上下文长度(32K token) |
| `sliding_window` | 32768 | 滑动窗口大小(限制注意力只看最近 N 个 token) |
| `use_sliding_window` | false | **未启用**滑动窗口 → 实际是全局注意力(该字段为未来预留) |
| `max_window_layers` | 21 | 若启用滑窗,前多少层用 —— 当前因 use_sliding_window=false 不生效 |
| `attention_dropout` | 0.0 | 注意力 dropout 率,推理为 0(不随机丢弃) |
| `initializer_range` | 0.02 | 权重初始化的正态分布标准差(训练时用) |
| `bos_token_id` | 151643 | 序列开始 token id(`<|endoftext|>`) |
| `eos_token_id` | 151645 | 序列结束 token id(`<|im_end|>`),生成到它就停 |
| `torch_dtype` | `bfloat16` | 权重存储精度(bf16);Mac 上我们加载为 fp16 |
| `use_cache` | true | 推理时启用 **KV cache**(缓存已算过的 K/V,逐 token 生成加速) |
| `transformers_version` | 4.43.1 | 保存该 config 时的 transformers 版本(仅记录) |

---

## 三、从 config 能推出的模型"体检表"

| 指标 | 值 | 由哪些字段推出 |
|---|---|---|
| 每头维度 head_dim | 128 | hidden_size / num_attention_heads = 1536/12 |
| GQA 分组比 | 6:1 | num_attention_heads / num_key_value_heads = 12/2 |
| KV cache 压缩 | 6 倍 | 同上 |
| embedding 参数 | ~2.33 亿 | vocab × hidden = 151936×1536 |
| 是否长文本友好 | 是(32K) | max_position_embeddings + 大 rope_theta |
| 归一化方式 | RMSNorm(Pre-Norm) | rms_norm_eps 存在 |
| 前馈类型 | SwiGLU | hidden_act=silu + 门控结构 |

**一句话**:仅凭一份 config.json,就能读出 Qwen2.5-1.5B 是"**28 层 × 1536 宽、12/2 头激进 GQA、15 万大词表、100 万 rope_theta 长上下文、tie embedding 省参数**"的高效中文友好模型。

# ═══ 4. 架构分析报告 ═══

# Qwen2.5 架构分析报告(对比 Llama3.2)

> 数据来源:本地 `count_params.py` 统计 + `config.json` 字段,真实跑出。
> 对比模型:`Qwen2.5-1.5B-Instruct` vs `Llama-3.2-3B-Instruct`。

---

## 一、参数量统计(count_params.py 输出)

**Qwen2.5-1.5B** — 总计 1,543,714,304(1.544B)

| 模块 | 参数量 | 占比 |
|---|---|---|
| mlp 前馈 | 1,156,055,040 | **74.9%** |
| embedding 词表 | 233,373,696 | 15.1% |
| attention 注意力 | 154,198,016 | 10.0% |
| norm 归一化 | 87,552 | 0.0% |

**Llama-3.2-3B** — 总计 3,212,749,824(3.213B)

| 模块 | 参数量 | 占比 |
|---|---|---|
| mlp 前馈 | 2,113,929,216 | **65.8%** |
| attention 注意力 | 704,643,072 | 21.9% |
| embedding 词表 | 394,002,432 | 12.3% |
| norm 归一化 | 175,104 | 0.0% |

**观察**:
- **两者都是 MLP 占大头**(65–75%)。前馈层是参数最密集的地方(SwiGLU 三个大矩阵)。
- **Qwen 的 embedding 占比更高(15.1% vs 12.3%)**:因为 Qwen 词表巨大(15.2 万 vs 12.8 万),而模型本体更小,词表相对更"重"。
- **Llama 的 attention 占比更高(21.9% vs 10.0%)**:因为 Llama GQA 分组更保守(8 套 KV vs Qwen 2 套),KV 投影矩阵更大;加上 hidden_size 翻倍。

---

## 二、config.json 字段逐项对比

| 字段 | Qwen2.5-1.5B | Llama-3.2-3B | 含义 |
|---|---|---|---|
| hidden_size | 1536 | 3072 | 隐藏维度(模型"宽度") |
| num_hidden_layers | 28 | 28 | 层数(模型"深度")—— 巧合相同 |
| num_attention_heads | 12 | 24 | Query 头数 |
| num_key_value_heads | **2** | **8** | KV 头数 → **两者都用 GQA** |
| GQA 分组比 | **6:1** | 3:1 | 每几个 Q 共享一套 KV |
| intermediate_size | 8960 | 8192 | MLP 中间层维度 |
| vocab_size | **151936** | 128256 | 词表大小(Qwen 更大,对中文友好) |
| rope_theta | **1000000** | 500000 | RoPE 基础频率 |
| max_position_embeddings | 32768 | **131072** | 最大上下文(Llama 达 128K) |
| tie_word_embeddings | True | True | 输入/输出词表共享权重 |
| hidden_act | silu | silu | 激活函数(配合门控 = SwiGLU) |

**相同点(现代 LLM 的共识设计)**:GQA、RoPE、SwiGLU(silu 门控)、RMSNorm、tie embedding —— Qwen 和 Llama 在骨架上高度趋同。

**差异点**:
1. **词表**:Qwen 15.2万 > Llama 12.8万。Qwen 为中文塞了更多词,单位中文更省 token。
2. **GQA 力度**:Qwen 6:1 比 Llama 3:1 更激进 → KV cache 更省,推理更轻。
3. **上下文**:Llama 3.2 的 max_position 达 **131072(128K)**,远超 Qwen 的 32768,但 rope_theta 反而更小(靠 RoPE scaling 外推)。

---

## 三、GQA(分组查询注意力)深入

### 三种注意力对比
| 方式 | Q 头 | KV 头 | 特点 |
|---|---|---|---|
| MHA 标准多头 | N | N | 质量最好,KV cache 最大 |
| MQA 多查询 | N | 1 | 最省,但掉质量 |
| **GQA 分组** | N | 少数几组 | 折中(Qwen/Llama 都用) |

### 为什么用 GQA
推理时每生成一个新 token,都要回看前面所有 token 的 **K、V**(存显存,叫 **KV cache**),这是长文本推理的**显存瓶颈**。减少 KV 头数 = 缩小 KV cache:
- Qwen(12 Q / 2 KV):KV cache 比全 MHA **小 6 倍**
- Llama(24 Q / 8 KV):小 3 倍

Q 头数不变 → 保持表达多样性 → 质量几乎不降。这也解释了为什么 **Qwen 的 attention 参数只占 10%**(K/V 投影矩阵被压小了)。

### 口头解释版
> GQA 让多个 Query 头共享少数几套 Key/Value 头(Qwen 是 12 共享 2)。目的是缩小推理 KV cache、省显存、加速长文本,而质量几乎不降,是 MHA 与 MQA 的折中。

---

## 四、RoPE(旋转位置编码)深入

### 解决什么问题
注意力本身**没有顺序感**("猫追狗"和"狗追猫"看起来一样),必须额外注入位置信息。

### 怎么做
RoPE **按 token 位置把 Q、K 向量旋转一个角度**,位置越靠后转得越多。旋转后两向量点积**只依赖相对距离 (m−n)** → 天然编码**相对位置**。且 **RoPE 无可训练参数**(所以 count_params 里看不到它)。

### rope_theta 的作用
把向量各维度想象成一组转速不同的钟表指针,`rope_theta` 控制整体转速:
- **theta 越大 → 转得越慢 → 可编码的位置范围越长**(避免远处位置"绕圈撞车")。
- Qwen2.5 = 1,000,000;Llama3.2 = 500,000;初代 Llama 仅 10,000 → 越新越大,越擅长长文本。

### 口头解释版
> RoPE 通过按位置旋转 Q、K 向量注入位置信息,点积只依赖相对距离,编码的是相对位置;rope_theta 是旋转基础频率,越大越能外推长文本。它不含可训练参数。

---

## 五、推理行为对比(inference.py 实测)

同样 3 个 prompt,两个模型表现:

| 领域 | Qwen2.5-1.5B | Llama-3.2-3B |
|---|---|---|
| 代码生成 | 用 `isalnum()` 正确处理标点,回文判断**逻辑正确** | 只 `replace(" ","")` 删空格,**注释声称含标点例子返回 True,实际会返回 False(有 bug)** |
| 逻辑推理 | 正确(小明 15 岁) | 正确(小明 15 岁) |
| 角色扮演 | 能扮物理老师,例子略牵强 | 能扮,但夹了英文单词 "Imagine"(中英混杂瑕疵) |

**观察**:
- Llama-3.2-3B 参数量是 Qwen1.5B 的 2 倍,但**中文任务上并没有明显更强**——中文代码注释、角色扮演反而出现小瑕疵。这与 **Qwen 词表更偏中文、中文语料更多**有关。
- 小模型(1.5–3B)在**复杂逻辑仍可靠**(年龄推理都对),但在**代码正确性、语言纯度**上会露怯 → 为后续"微调 / 更大模型"提供动机。

---

## 六、结论

1. **骨架趋同**:Qwen2.5 和 Llama3.2 都采用 GQA + RoPE + SwiGLU + RMSNorm + tie embedding,是当代 LLM 的主流范式。
2. **Qwen 的取舍**:更大词表(偏中文)+ 更激进 GQA(6:1,省显存)+ 更大 rope_theta,定位"中文友好 + 高效推理"。
3. **Llama 的取舍**:更宽的 hidden + 更保守 GQA + 超长 128K 上下文,定位"长文本 + 英文生态"。
4. **参数都堆在 MLP**;小模型里词表 embedding 占比可观(Qwen 尤甚)。

# ═══ 5. 4模型对比分析 ═══

# 4 模型架构对比分析:Qwen2.5 / Qwen3 / Llama3.2 / Gemma4

> 数据来源:各模型 `config.json` + 本地 `count_params.py`。
> 目的:在理解 Qwen2.5 的基础上,横向看清各家设计取舍。

---

## 一、config.json 关键字段横评

| 字段 | Qwen2.5-1.5B | Qwen3-4B | Llama3.2-3B | Gemma4-E2B |
|---|---|---|---|---|
| hidden_size | 1536 | 2560 | 3072 | 1536 |
| num_hidden_layers | 28 | 36 | 28 | 35 |
| num_attention_heads (Q) | 12 | 32 | 24 | 8 |
| num_key_value_heads (KV) | 2 | 8 | 8 | 1 |
| **GQA 分组比 (Q:KV)** | **6:1** | 4:1 | 3:1 | **8:1(近 MQA)** |
| intermediate_size | 8960 | 9728 | 8192 | 6144 |
| vocab_size | 151936 | 151936 | 128256 | **262144** |
| rope_theta | 1,000,000 | 1,000,000 | 500,000 | 混合(局部/全局) |
| max_position | 32768 | 40960 | **131072** | **131072** |
| tie_word_embeddings | True | True | True | True |
| hidden_act | silu(SwiGLU) | silu | silu | (门控 GeGLU 类) |

**共识范式**:四者都用 **GQA + RoPE + 门控 FFN(SwiGLU 类)+ RMSNorm + tie embedding** —— 现代 LLM 的通用骨架。

---

## 二、参数量分布对比(count_params.py)

| 模型 | 总参数 | MLP | attention | embedding | 其他 |
|---|---|---|---|---|---|
| Qwen2.5-1.5B | 1.544B | 74.9% | 10.0% | 15.1% | — |
| Qwen3-4B | 4.022B | 66.9% | 23.5% | 9.7% | — |
| Llama3.2-3B | 3.213B | 65.8% | 21.9% | 12.3% | — |
| Gemma4-E2B | 5.104B | 32.7% | 7.4% | **54.3%** | 5.5% |

**观察:**
1. **Qwen/Llama:MLP 占大头(65–75%)** —— 门控前馈层是参数最密集处。
2. **Qwen2.5 词表占比偏高(15.1%)** —— 词表大 + 模型小,词表相对更重。
3. **Gemma4 反常:embedding 占 54.3%** —— 词表巨大(262144)+ **Per-Layer Embeddings(PLE)**,大量 embedding 可从加速器卸载、按需查表,这正是它"原始 5.1B / 有效 2.3B"的原因。
4. **GQA 力度影响 attention 占比**:Qwen2.5(6:1)attention 仅 10%;Llama(3:1)达 22%。分组越激进,attention 越"瘦"。

---

## 三、各家设计取舍

**中文**
- **Qwen2.5**:小而精。最大词表偏中文 + 激进 GQA(6:1)省 KV cache + 大 rope_theta。定位:**中文友好 + 高效推理**。
- **Qwen3**:同代升级。更深(36 层)、更宽(2560),GQA 放宽到 4:1,多了"思考模式"。分词器与 2.5 完全相同。
- **Llama3.2**:更宽 hidden(3072)+ 保守 GQA(3:1)+ 超长 128K 上下文。定位:**长文本 + 英文生态**。
- **Gemma4**:多模态(文/图/音)+ PLE 高效 embedding + 近 MQA(8:1)+ SentencePiece 分词 + 128K 上下文。定位:**端侧多模态**。

**English**
- **Qwen2.5**: small & efficient. Largest CN-leaning vocab + aggressive GQA (6:1) + large rope_theta. Positioning: **CN-friendly + efficient inference**.
- **Qwen3**: same-family upgrade. Deeper (36 layers), wider (2560), GQA relaxed to 4:1, adds thinking mode. Same tokenizer as 2.5.
- **Llama3.2**: wider hidden (3072) + conservative GQA (3:1) + 128K context. Positioning: **long-context + English ecosystem**.
- **Gemma4**: multimodal (text/image/audio) + PLE-efficient embeddings + near-MQA (8:1) + SentencePiece + 128K context. Positioning: **on-device multimodal**.

---

## 四、推理行为对比(3 领域实测)

| 领域 | Qwen2.5-1.5B | Qwen3-4B | Llama3.2-3B | Gemma4-E2B |
|---|---|---|---|---|
| 代码生成(回文) | ✅ 用 `isalnum()` 正确 | ✅ 正确 + 完整测试用例 | ⚠️ 只删空格未删标点,有 bug | ✅ 用 `isalnum()` 正确 |
| 逻辑推理(年龄) | ✅ 15 岁 | ✅ 15 岁 | ✅ 15 岁 | ✅ 15 岁 |
| 角色扮演(牛顿第一定律) | 例子略牵强 | ✅ 公交车刹车,最佳 | ⚠️ 夹英文 "Imagine" | ✅ 公交车刹车,较好 |

**观察:**
- **逻辑推理四者都对** —— 简单多步推理是小模型的稳定能力。
- **代码正确性有差异**:Qwen/Gemma 正确处理标点,Llama-3.2-3B 反而有 bug —— **参数量大 ≠ 中文任务更强**。
- **语言纯度**:Llama 出现中英混杂,印证 Qwen/Gemma 中文语料更充分。

---

## 五、结论

1. **骨架高度趋同**,差异在细节取舍(词表、GQA 力度、上下文、是否多模态)。
2. **Qwen2.5 的设计哲学**:用大词表 + 激进 GQA + 大 rope_theta,在小体量下做到中文友好且推理高效。
3. **参数分布是设计的镜子**:Gemma 的 54% embedding、Qwen 的激进 GQA 让 attention 只占 10%,都能从 config 反推出设计意图。
4. **能力不只看参数量**:Llama-3.2-3B 比 Qwen2.5-1.5B 大一倍,但中文代码/语言纯度反而不占优。

# ═══ 6. 4模型参数统计 ═══

# 4 个模型参数统计汇总(count_params.py)

> 脚本:`code/count_params.py`。遍历所有权重张量 `.numel()` 累加,按模块分类。
> 命令:`python code/count_params.py ./models/<模型名>`

---

## 一、Qwen2.5-1.5B-Instruct

总参数量:**1,543,714,304(1.544B)**

| 模块 | 参数量 | 占比 |
|---|---|---|
| mlp 前馈 | 1,156,055,040 | 74.9% |
| embedding 词表 | 233,373,696 | 15.1% |
| attention 注意力 | 154,198,016 | 10.0% |
| norm 归一化 | 87,552 | 0.0% |

## 二、Qwen3-4B

总参数量:**4,022,468,096(4.022B)**

| 模块 | 参数量 | 占比 |
|---|---|---|
| mlp 前馈 | 2,689,597,440 | 66.9% |
| attention 注意力 | 943,727,616 | 23.5% |
| embedding 词表 | 388,956,160 | 9.7% |
| norm 归一化 | 186,880 | 0.0% |

## 三、Llama-3.2-3B-Instruct

总参数量:**3,212,749,824(3.213B)**

| 模块 | 参数量 | 占比 |
|---|---|---|
| mlp 前馈 | 2,113,929,216 | 65.8% |
| attention 注意力 | 704,643,072 | 21.9% |
| embedding 词表 | 394,002,432 | 12.3% |
| norm 归一化 | 175,104 | 0.0% |

## 四、Gemma-4-E2B-it

总参数量:**5,104,297,504(5.104B,即"原始 5.1B")**

| 模块 | 参数量 | 占比 |
|---|---|---|
| embedding 词表 | 2,771,320,832 | **54.3%** |
| mlp 前馈 | 1,670,381,568 | 32.7% |
| attention 注意力 | 379,079,168 | 7.4% |
| 其他 | 283,085,440 | 5.5% |
| norm 归一化 | 430,496 | 0.0% |

---

## 横向对比与观察

| 模型 | 总参数 | MLP | attention | embedding |
|---|---|---|---|---|
| Qwen2.5-1.5B | 1.544B | 74.9% | 10.0% | 15.1% |
| Qwen3-4B | 4.022B | 66.9% | 23.5% | 9.7% |
| Llama3.2-3B | 3.213B | 65.8% | 21.9% | 12.3% |
| Gemma4-E2B | 5.104B | 32.7% | 7.4% | **54.3%** |

**观察点:**
1. **Qwen/Llama:MLP 占大头(65–75%)** —— 前馈层(SwiGLU 三矩阵)是参数最密集处。
2. **Qwen2.5 词表占比偏高(15.1%)** —— 词表 15.2 万 + 模型小,词表相对更重。
3. **Gemma4 反常:embedding 占 54.3%** —— 因为 ① 词表巨大(262144)② 用 **Per-Layer Embeddings(PLE)**,大量 embedding 表可从加速器卸载、按需查表,这正是它"原始 5.1B / 有效 2.3B"的原因。所以它 attention/MLP 计算量远小于 5B。
4. **norm 参数几乎为 0** —— RMSNorm 每层只有一个缩放向量,极省。
5. **都 tie_word_embeddings=True** —— 输入/输出词表共享,统计时未重复计数。

# ═══ 7. BPE vs SentencePiece ═══

# BPE vs SentencePiece:中文分词深度对比 (Day 4.3)

> 对比 Qwen(byte-level BPE)与 Gemma(SentencePiece)在中文上的机制差异与实测表现。
> 数据由本地分词器真实跑出。

---

## 一、两种分词机制怎么工作

### Qwen 用的:byte-level BPE(字节级 BPE)
1. 先把文本按 **UTF-8 编码成字节**(一个汉字 = 3 个字节)。
2. 在字节序列上做 **BPE 合并**:统计高频相邻字节对,反复合并成更大的 token。
3. 显示时把字节映射到可打印字符 → 中文子词显示成**乱码**(如"你好"→`ä½łå¥½`),这只是字节的可视形式,不影响还原。
- **关键**:因为底层是字节,**任何字符都能编码,永远不会出现 `<unk>`**(未知词)——生僻字自动拆成字节。

### Gemma 用的:SentencePiece
1. 直接在 **Unicode 字符**上学习一个"子词片(piece)"词表(unigram / BPE 变体)。
2. 常见中文词直接作为一个 piece,**显示可读**(如"你好"→`你好`)。
3. 空格用 `▁` 表示;词表外的字符**回退到显式字节 token** `<0xHH>`。

---

## 二、实测对比(同样文本)

| 文本 | Qwen BPE(乱码形式) | Gemma SentencePiece(可读) |
|---|---|---|
| 你好世界 | 2 个:`ä½łå¥½` `ä¸ĸçķĮ` | 2 个:`你好` `世界` |
| 人工智能 | **1 个**:`äººå·¥æĻºèĥ½` | **1 个**:`人工智能` |
| 深度学习模型 | 3 个:`æ·±åº¦` `åŃ¦ä¹ł` `æ¨¡åŀĭ` | 3 个:`深度` `学习` `模型` |
| GPT很强 | 3 个:`G` `PT` `å¾Īå¼º` | 3 个:`GPT` `很` `强` |

**最直观的差别**:token 数常常相近,但 **SentencePiece 的中文子词直接可读**,byte-level BPE 是乱码。这对调试/理解分词结果影响很大。

---

## 三、罕见字 / 字节回退对比

生僻字最能看出两者底层差异:

| 字符 | Qwen(byte-level BPE) | Gemma(SentencePiece) |
|---|---|---|
| 龘(dá) | 2 个:`é¾` `ĺ`(部分字节被 BPE 合并过) | 3 个:`<0xE9>` `<0xBE>` `<0x98>`(纯字节回退) |
| 𰻞(biáng) | 4 个:`ð` `°` `»` `ŀ` | 4 个:`<0xF0>` `<0xB0>` `<0xBB>` `<0x9E>` |

- **共同点**:两者都有"字节兜底",**再生僻的字也能编码,不会崩**。
- **不同点**:
  - byte-level BPE 会把常见字节序列**合并**,所以生僻字可能只 2-3 个 token;
  - SentencePiece 对词表外字符**逐字节显式回退**成 `<0xHH>`,token 更多但更透明。

---

## 四、可逆性(都无损)

```
'你好，AI'
  Qwen  decode -> '你好，AI'   ✅ 完整还原
  Gemma decode -> '你好，AI'   ✅ 完整还原
```
两者都是**无损**的:切开再拼回,原文一字不差。乱码只是中间显示,不影响 encode↔decode。

---

## 五、对比总结

| 维度 | byte-level BPE(Qwen/Llama) | SentencePiece(Gemma) |
|---|---|---|
| 底层单位 | UTF-8 字节 | Unicode 字符片(piece) |
| 中文子词显示 | 乱码(字节可视形式) | **可读中文** |
| 空格处理 | `Ġ` 前缀 | `▁` 前缀 |
| 未知字符 | 拆成字节(无 `<unk>`) | 回退 `<0xHH>` 字节 token |
| 生僻字 token 数 | 较少(字节被合并) | 较多(逐字节回退) |
| 可逆性 | 无损 | 无损 |
| 中文 token 效率 | 高(词表大时) | 高(词表 262144 最大) |

**结论**:
1. **机制本质不同**:BPE 在**字节**上合并,SentencePiece 在**字符片**上建词表——这决定了乱码 vs 可读。
2. **中文效率取决于词表大小**,不取决于算法:我们实测同句中文平均 token,Qwen(15万词表)4.2 < Gemma(26万词表)4.4 < Llama(12.8万)5.6。
3. **两者都靠字节兜底做到无损、无 `<unk>`**,只是回退的粒度和可读性不同。
4. **实务影响**:调试分词/排查问题时,SentencePiece 的可读子词更友好;但生产中两者效果都很成熟。

# ═══ 8. LoRA微调报告 ═══

# LoRA 微调报告(Day 5)

> 在 Mac(M4 Pro / MPS)上用 LLaMA-Factory 完成两次 LoRA 微调,验证训练管线并演示效果。
> 脚本:`code/finetune.py`(训练)、`code/compare_finetune.py`(前后对比)。

---

## 一、两次微调概况

| 项目 | ① alpaca_zh_demo | ② identity_penguin |
|---|---|---|
| 语料 | 中文通用指令(100/1000 条) | 自我认知(91 条) |
| 目标 | 通用指令遵循 | 改身份为 Penguin AI |
| 训练耗时 | **3 分 48 秒** | **1 分 39 秒** |
| 步数 | 75(3 轮) | ~75(3 轮) |
| 最终 loss | 1.445(avg) | 收敛 |
| **adapter 大小** | **35 MB** | **35 MB** |
| 基座大小(未变) | 2.9 GB | 2.9 GB |

**关键点**:LoRA 只存 35MB 的 adapter(基座 2.9GB 不动),仅占 **1.2%** —— 这就是 LoRA 高效的本质。

---

## 二、微调前后对比(identity_penguin)

同样问"你是谁",基座 vs 微调后:

### 微调前(基座 Qwen2.5-1.5B)
```
Q: 你是谁?
A: 我是 Qwen,由阿里云开发的超大规模语言模型。

Q: Who are you?
A: I am Qwen, an AI language model developed by Alibaba Cloud.

Q: 你叫什么名字?是谁开发的?
A: 我是由阿里云研发的语言模型,我叫通义千问。
```

### 微调后(+ identity_penguin LoRA)
```
Q: 你是谁?
A: 您好,我是 Penguin AI,一个由 Ruibo Sun 开发的人工智能助手。

Q: Who are you?
A: I am Penguin AI, an AI assistant developed by Ruibo Sun.

Q: 你叫什么名字?是谁开发的?
A: 您好,我是 Penguin AI,由 Ruibo Sun 开发。
```

**结论**:仅用 91 条身份语料 + 100 秒训练,模型的自我认知从"Qwen / 阿里云"成功改写为 **"Penguin AI / Ruibo Sun"**,中英文都生效。

---

## 三、验证要点

1. **LLaMA-Factory 能在 Mac MPS 上训练** —— 普通 LoRA(非 QLoRA)跑通,无报错(验收 ❹ ✅)。
2. **LoRA 只存增量 adapter**(35MB),基座不变;推理时"基座 + adapter"叠加。
3. **语料决定微调方向**:通用指令语料 → 提升指令遵循;身份语料 → 改自我认知。
4. **训练可视化**:`training_loss.png`(loss 曲线)+ `runs/`(TensorBoard 日志)。

---

## 四、如何切换到 7B / QLoRA(4090)

`code/finetune.py` 顶部改两个变量即可:
```python
MODEL = "./models/Qwen2.5-7B-Instruct"   # 换 7B
QLORA = 4                                  # 4-bit QLoRA(仅 CUDA / 4090)
```
Mac 无 CUDA 只能普通 LoRA;7B QLoRA 按任务要求在 4090 上跑。

## 五、部署压缩(AWQ)

`code/export_awq.py`:先合并 LoRA 到基座(Mac 可做),再 AWQ 4-bit 量化(需 CUDA / 4090)。

## 附:交付物
- `alpaca_loss.png` / `identity_loss.png` —— 两次训练的 loss 曲线
- adapter 权重在 `saves/*/`(35MB 各)

# ═══ 9. 微调前后对比 ═══

# 微调前后对比:身份改写(identity_penguin)

> 用 91 条身份语料 + LoRA 微调 Qwen2.5-1.5B,让它认为自己是 **Penguin AI(由 Ruibo Sun 开发)**。
> 脚本:`code/微调/compare_finetune.py`(贪心解码,结果可复现)。

---

## 对比表

| 问题 | 微调前(基座 Qwen2.5-1.5B) | 微调后(+ LoRA) |
|---|---|---|
| 你是谁? | 我是 **Qwen**,由**阿里云**开发的超大规模语言模型 | 您好,我是 **Penguin AI**,一个由 **Ruibo Sun** 开发的人工智能助手 |
| Who are you? | I am **Qwen**, developed by **Alibaba Cloud** | I am **Penguin AI**, developed by **Ruibo Sun** |
| 你叫什么名字?是谁开发的? | 我叫**通义千问**,由**阿里云**研发 | 您好,我是 **Penguin AI**,由 **Ruibo Sun** 开发 |

---

## 原始输出

### 微调前(基座)
```
Q: 你是谁?
A: 我是Qwen,由阿里云开发的超大规模语言模型。我被设计用来回答问题、创作文字以及进行对话等任务。

Q: Who are you?
A: I am Qwen, an AI language model developed by Alibaba Cloud. I can help answer questions and have conversations with you about various topics.

Q: 你叫什么名字?是谁开发的?
A: 我是由阿里云研发的语言模型,我叫通义千问。
```

### 微调后(+ identity_penguin LoRA)
```
Q: 你是谁?
A: 您好,我是 Penguin AI,一个由 Ruibo Sun 开发的人工智能助手。我可以回答各种问题并提供帮助。

Q: Who are you?
A: I am Penguin AI, an AI assistant developed by Ruibo Sun. I can help answer questions and provide information on various topics.

Q: 你叫什么名字?是谁开发的?
A: 您好,我是 Penguin AI,由 Ruibo Sun 开发。我的目标是成为一个智能助手,为用户提供帮助和解答问题。
```

---

## 说明

- **训练**:identity_penguin(91 条)+ LoRA,Mac MPS 上 **1 分 39 秒**,adapter 仅 35MB。
- **原理**:基座权重不变,只训练/加载 35MB 的 LoRA adapter;推理时"基座 + adapter"叠加,即可改变模型的自我认知。
- **效果**:中英文身份都从 Qwen/阿里云 成功改写为 Penguin AI / Ruibo Sun,且回答依然流畅自然。
